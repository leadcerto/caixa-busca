/**
 * ARQUIVO DE ROTAS DO MÓDULO DE BUSCA DE IMÓVEIS
 * IA (Antigravity): Converta isso para o padrão do framework utilizado.
 */

// Importa o Controller do módulo
// const BuscaController = require('../Controllers/BuscaController');

/**
 * @route GET /api/imoveis/buscar
 * @desc Retorna lista paginada de imóveis baseada em filtros de Query String
 * @access Public
 */
// router.get('/buscar', BuscaController.pesquisarImoveis);

/**
 * @route GET /api/imoveis/:id
 * @desc Retorna os detalhes de 1 imóvel específico MESCLADO com o Dossiê do Bairro
 * @access Public
 */
// router.get('/:id', BuscaController.detalhesImovel);

