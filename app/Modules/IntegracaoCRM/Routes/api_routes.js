/**
 * ARQUIVO DE ROTAS DO MÓDULO DE INTEGRAÇÃO CRM
 * IA (Antigravity): Converta isso para o padrão do framework utilizado.
 */

// Importa o Controller do módulo
// const CrmController = require('../Controllers/CrmController');

/**
 * @route POST /api/crm/capturar-lead
 * @desc Recebe os dados do lead (Nome, WhatsApp, Email, ID do Imóvel)
 * @access Public
 */
// router.post('/capturar-lead', CrmController.receberLead);

/**
 * @route POST /api/crm/sincronizar-pendentes
 * @desc Rota interna/Cron para processar leads que falharam no envio anterior
 * @access Private
 */
// router.post('/sincronizar-pendentes', CrmController.sincronizarFalhas);

